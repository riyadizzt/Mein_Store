# =============================================================
# MALAK STORE — PRODUCTION ENVIRONMENT
# ACHTUNG: Hier fließt echtes Geld!
# Alle TEST Keys durch LIVE Keys ersetzen!
# =============================================================

# ─── DATABASE (Railway PostgreSQL) ────────────────────────
DATABASE_URL=REPLACE_WITH_RAILWAY_POSTGRES_URL
DIRECT_URL=REPLACE_WITH_RAILWAY_POSTGRES_DIRECT_URL

# ─── REDIS (Upstash EU Production) ───────────────────────
UPSTASH_REDIS_REST_URL=REPLACE_WITH_UPSTASH_URL
UPSTASH_REDIS_REST_TOKEN=REPLACE_WITH_UPSTASH_TOKEN

# ─── JWT (generiere neue Secrets für Production!) ─────────
# openssl rand -hex 64
JWT_SECRET=REPLACE_GENERATE_WITH_openssl_rand_hex_64
JWT_REFRESH_SECRET=REPLACE_GENERATE_WITH_openssl_rand_hex_64
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ─── STRIPE (LIVE MODE!) ─────────────────────────────────
STRIPE_SECRET_KEY=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
STRIPE_WEBHOOK_SECRET=whsec_r6GPFB2adGIDL5OP8LtanHcSs0WaH3i0
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_51NlEtCJRub7L8rXv83JKyXQbUg3RsBPI1IyqXReuDl40P13TYkt5p5z28zvzPmz7yNu95SkY3yypwHi8VBgOcNod00jVha3dFf

# ─── KLARNA (PRODUCTION) ─────────────────────────────────
KLARNA_API_URL=https://api.klarna.com
KLARNA_USERNAME=REPLACE
KLARNA_PASSWORD=REPLACE
KLARNA_WEBHOOK_SECRET=REPLACE

# ─── DHL (PRODUCTION) ────────────────────────────────────
DHL_API_KEY=REPLACE
DHL_API_SECRET=REPLACE
DHL_ACCOUNT_NUMBER=REPLACE_GESCHAEFTSKUNDENNUMMER
DHL_SANDBOX=false
DHL_WEBHOOK_SECRET=REPLACE

# ─── EMAIL (RESEND — verifizierte Domain!) ────────────────
RESEND_API_KEY=re_bsmaC4FU_Mwzd1dkogX8assuPF3SC23KS
EMAIL_FROM_NOREPLY=noreply@malak-bekleidung.com
EMAIL_FROM_ORDERS=bestellungen@malak-bekleidung.com
EMAIL_FROM_SUPPORT=support@malak-bekleidung.com
EMAIL_DISPLAY_NAME=Malak Bekleidung

# ─── COMPANY ──────────────────────────────────────────────
COMPANY_NAME=REPLACE_NACH_GEWERBEANMELDUNG
COMPANY_ADDRESS=REPLACE
COMPANY_VAT_ID=REPLACE
COMPANY_CEO=REPLACE
COMPANY_REGISTER=REPLACE
COMPANY_PHONE=REPLACE
COMPANY_CONTACT_EMAIL=info@malak-bekleidung.com
COMPANY_LOGO_URL=REPLACE_CLOUDINARY_URL

# ─── CLOUDINARY ───────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=REPLACE
CLOUDINARY_API_KEY=REPLACE
CLOUDINARY_API_SECRET=REPLACE
CLOUDINARY_URL=cloudinary://KEY:SECRET@NAME

# ─── APP URLs ─────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://malak-bekleidung.com
NEXT_PUBLIC_API_URL=https://api.malak-bekleidung.com
APP_URL=https://malak-bekleidung.com
API_PORT=3001

# ─── MONITORING ───────────────────────────────────────────
SENTRY_DSN=REPLACE

# ─── MISC ─────────────────────────────────────────────────
RESERVATION_TIMEOUT_MINUTES=15
SHIPPING_PROVIDER=zone_based
NODE_ENV=production
